def filter_incorrect():
    list = []
    with open("lottery_numbers.csv") as file:
        for line in file:
            line = line.replace("\n", "")
            parts = line.split(";")
            week = parts[0].split()[1]
            nums = parts[1].split(",")
        if week.isdigit() and n.isdigit()(for n in nums)
    with open("correct_numbers.csv", "a") as new:

            


            